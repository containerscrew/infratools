name: Build 🐛

on:
  pull_request:
  push:
    branches:
      - "**"

jobs:
  build:
    name: Build for ${{ matrix.platform.release_for }}
    strategy:
      matrix:
        platform:
          - release_for: linux-arm64
            os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            targetarch: arm64

          - release_for: linux-amd64
            os: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
            targetarch: amd64

    runs-on: ${{ matrix.platform.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build full image
        uses: docker/build-push-action@v6
        with:
          target: full
          push: false
          tags: docker.io/containerscrew/infratools:testing
          build-args: |
            ARCH=${{ matrix.platform.targetarch }}

      - name: Build ci image
        uses: docker/build-push-action@v6
        with:
          target: ci
          push: false
          tags: docker.io/containerscrew/infratools:testing-ci
          build-args: |
            ARCH=${{ matrix.platform.targetarch }}
